plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android" // Kotlin plugin burada ekleniyor
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.archive.app2.archive_app2"
    compileSdk = 34
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17" // Burada JavaVersion.VERSION_17 yerine "17" kullanılıyor
    }

    defaultConfig {
        applicationId = "com.archive.app2.archive_app2"
        minSdk = 21
        targetSdk = 34
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
